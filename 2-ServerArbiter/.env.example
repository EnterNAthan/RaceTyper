# Credentials PostgreSQL (dev local) - identiques à docker-compose.yml
# User: racetyper | Password: racetyper | DB: racetyper
DATABASE_URL=postgresql+asyncpg://racetyper:racetyper@127.0.0.1:5433/racetyper

# Optionnel : activer les requêtes SQL en log (0 ou 1)
# SQL_ECHO=0
